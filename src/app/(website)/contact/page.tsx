import React from 'react';
import { ContactView } from '@/components/ContactView';

export const metadata = {
  title: 'Family Concierge & Support | CRUNIQUE',
  description: 'Reach out to CRUNIQUE Family Concierge for order tracking, corporate hampers, and customer support.',
};

export default function ContactPage() {
  return <ContactView />;
}
