import React from 'react';
import { AboutView } from '@/components/AboutView';

export const metadata = {
  title: 'Our Family Heritage & Story | CRUNIQUE',
  description: 'Learn about CRUNIQUE family values, 100% real fruit commitment, and low-temperature vacuum crunch technology.',
};

export default function AboutPage() {
  return <AboutView />;
}
